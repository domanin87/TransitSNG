import React, {useEffect,useState} from 'react'; import api from '../utils/api'; export default function Chat(){ const [msgs,setMsgs]=useState([]); useEffect(()=>{ api.get('/api/messages/demo').then(r=>setMsgs(r.data||r)); },[]); return (<div className='container'><h2>Чат (демо)</h2>{msgs.map(m=>(<div key={m.id} className='card' style={{marginTop:8}}><b>{m.from_user}</b><div>{m.text}</div></div>))}</div>); }
