import React, {useEffect,useState} from 'react'; import api from '../utils/api'; export default function CargoList(){ const [items,setItems]=useState([]); useEffect(()=>{ api.get('/api/cargo').then(r=>setItems(r.data||r)); },[]); return (<div className='container'><h2>Грузы</h2>{items.map(i=>(<div className='card' key={i.id}><h3>{i.title}</h3><div>{i.from_city} → {i.to_city}</div></div>))}</div>); }
