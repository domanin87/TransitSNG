import React, {useEffect,useState} from 'react'; import api from '../utils/api'; export default function TransportList(){ const [items,setItems]=useState([]); useEffect(()=>{ api.get('/api/transport').then(r=>setItems(r.data||r)); },[]); return (<div className='container'><h2>Транспорт</h2>{items.map(i=>(<div className='card' key={i.id}><h3>{i.title}</h3><div>{i.city} — {i.capacity_kg} kg</div></div>))}</div>); }
