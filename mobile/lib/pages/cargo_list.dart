import 'package:flutter/material.dart';

class CargoListPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Center(child: Text('Список грузов (похож на веб версию)', key: ValueKey('cargo')));
  }
}
